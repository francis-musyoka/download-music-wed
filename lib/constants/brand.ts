// Update this string to rebrand the site. All UI references — nav, hero,
// footer, page title — read from BRAND_NAME.
export const BRAND_NAME = "WaxMusic";

// Canonical production origin. Used for metadataBase, canonical URLs, the
// sitemap, robots.txt, and absolute OpenGraph image URLs. No trailing slash.
export const SITE_URL = "https://waxmusic.app";

// One-line product description reused across SEO metadata (page description,
// OpenGraph, Twitter card).
export const SITE_DESCRIPTION =
    "An open hit detector. Cut from curated playlists, stream-count signal, and a healthy distrust of algorithms.";
